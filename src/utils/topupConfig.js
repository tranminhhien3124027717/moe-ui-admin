// Configuration values for top-ups feature
export const topupConfig = {
  // Minimum minutes in advance for same-day scheduling (in minutes)
  // If schedule date is today, user can only schedule times after now + this value
  minScheduleAdvanceMinutes: 5,
};
